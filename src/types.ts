export type ContactKind = "schottky" | "ohmic" | "zero_field" | "fixed_potential";
export type ChargeMode = "mobile_carriers" | "fixed_volume";
export interface Layer { name:string; material:string; thickness_nm:number; alloy_fraction:number|null; donors_cm3:number; acceptors_cm3:number; sheet_charge_cm2:number; charge_mode:ChargeMode; fixed_charge_c_cm3:number; mesh_spacing_nm:number|null }
export interface Contact { kind:ContactKind; barrier_ev:number; voltage_v:number }
export interface Project { schema_version:number; name:string; description:string; temperature_k:number; mesh_spacing_nm:number; fully_ionized:boolean; layers:Layer[]; surface:Contact; substrate:Contact; quantum:{start_nm:number;stop_nm:number;states:number}|null; sweep:{enabled:boolean;start_v:number;stop_v:number;step_v:number}; solver:{max_iterations:number;tolerance:number;mixing:number;preview_scale:number}; analytic_verification:boolean }
export interface Eigenstate { index:number; energy_ev:number; wavefunction:number[] }
export interface AnalyticVerification { potential_v:number[]; electric_field_v_cm:number[]; max_potential_error_v:number; max_field_error_v_cm:number; oxide_voltage_v:number|null; semiconductor_voltage_v:number|null; oxide_field_v_cm:number|null; peak_semiconductor_field_v_cm:number|null; semiconductor_charge_c_cm2:number|null; balancing_sheet_charge_c_cm2:number }
export interface Result { position_nm:number[]; potential_v:number[]; electric_field_v_cm:number[]; conduction_band_ev:number[]; valence_band_ev:number[]; electron_cm3:number[]; hole_cm3:number[]; net_charge_cm3:number[]; charge_density_c_cm3:number[]; material:string[]; eigenstates:Eigenstate[]; sweep:{voltage_v:number;charge_c_m2:number;capacitance_f_m2:number|null;current_a_m2:number|null}[]; convergence:{converged:boolean;iterations:number;residual:number;quality:"preview"|"full";warnings:string[]}; analytic:AnalyticVerification|null }
